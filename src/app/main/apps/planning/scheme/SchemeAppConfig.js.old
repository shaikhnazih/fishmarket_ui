import React from 'react';

export const SchemeAppConfig = {
    schemes: {
        layout: {}
    },
    routes: [
  
        {
            path     : '/apps/planning/schemes/create/',
            component: React.lazy(() => import('./SchemeApp/SchemeApp'))
        },

        {
            path: '/apps/planning/schemes/',
            component: React.lazy(() => import('./SchemeApp/SchemeApp'))
        }

    ]
};


